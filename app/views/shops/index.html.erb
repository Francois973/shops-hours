<h1>Shop</h1>

<ul>
  <% @shops.each do |shop| %>
      <p><%= t("hours") %> :</p>
    <div class="container">
      <% shop.shop_hours.each do |hour| %>
        <div class="shop-hours <%= strongify(t("day.#{hour.day_of_week.to_s}")) ? 'bold-day' : '' %>">
          <li><%= t("day.#{hour.day_of_week.to_s}") %></li>
          <% hour.range_hours.each do |range| %>
            <%= display_hours_of_each_shop(range) %>
          <% end %>
        </div>
      <% end %>
    </div>
<% end %>
</ul>
